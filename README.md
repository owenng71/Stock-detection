
Clone the Repository

git clone <your-repo-link>

Then navigate into the project folder:

cd <your-repo>

Run the App

In case you don't have streamlit:

pip install streamlit


Then run:

streamlit run main.py
